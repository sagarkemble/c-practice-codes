#include<stdio.h>
int main()
{
    int capacity;
    int array[capacity];
    int

    printf("entercapacity of array");
    scanf("%d",capacity);
printf("enter a number to be stored in array ");
for (int i = 0; i <= capacity; i++)
{
 printf("enter a number to stored in array block %d",i+1);
 scanf("%d",)
   }

return 0;
}